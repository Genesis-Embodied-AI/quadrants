quadrants.lang.simt.block
=========================

.. py:module:: quadrants.lang.simt.block


Classes
-------

.. autoapisummary::

   quadrants.lang.simt.block.SharedArray


Functions
---------

.. autoapisummary::

   quadrants.lang.simt.block.arch_uses_spv
   quadrants.lang.simt.block.sync
   quadrants.lang.simt.block.sync_all_nonzero
   quadrants.lang.simt.block.sync_any_nonzero
   quadrants.lang.simt.block.sync_count_nonzero
   quadrants.lang.simt.block.mem_fence
   quadrants.lang.simt.block.mem_sync
   quadrants.lang.simt.block.thread_idx
   quadrants.lang.simt.block.global_thread_idx
   quadrants.lang.simt.block.reduce
   quadrants.lang.simt.block.reduce_all
   quadrants.lang.simt.block.reduce_add
   quadrants.lang.simt.block.reduce_min
   quadrants.lang.simt.block.reduce_max
   quadrants.lang.simt.block.reduce_all_add
   quadrants.lang.simt.block.reduce_all_min
   quadrants.lang.simt.block.reduce_all_max
   quadrants.lang.simt.block.inclusive_scan
   quadrants.lang.simt.block.exclusive_scan
   quadrants.lang.simt.block.inclusive_add
   quadrants.lang.simt.block.inclusive_min
   quadrants.lang.simt.block.inclusive_max
   quadrants.lang.simt.block.exclusive_add
   quadrants.lang.simt.block.exclusive_min
   quadrants.lang.simt.block.exclusive_max
   quadrants.lang.simt.block.radix_rank_match_atomic_or


Module Contents
---------------

.. py:function:: arch_uses_spv(arch)

.. py:function:: sync()

.. py:function:: sync_all_nonzero(predicate)

.. py:function:: sync_any_nonzero(predicate)

.. py:function:: sync_count_nonzero(predicate)

.. py:function:: mem_fence()

.. py:function:: mem_sync()

.. py:function:: thread_idx()

.. py:function:: global_thread_idx()

.. py:class:: SharedArray(shape, dtype)

   .. py:attribute:: dtype


   .. py:attribute:: shared_array_proxy


   .. py:method:: subscript(*indices)


.. py:function:: reduce(value, tid, block_dim: template(), log2_warp: template(), op: template(), dtype: template())

   Block-scope reduction under a generic associative ``op``.  Result valid in **thread 0 only**.  See `reduce_add`
   for the argument contract; this generic form additionally takes an ``op: template()`` ``@qd.func`` taking two args
   and returning the same type as ``value``, so callers can plug in custom monoids (bitwise ops, multiplicative,
   matrix-multiply, etc.) without re-implementing the warp-reduce + shared-mem skeleton.


.. py:function:: reduce_all(value, tid, block_dim: template(), log2_warp: template(), op: template(), dtype: template())

   Block-scope reduction under a generic associative ``op``, broadcast to every thread.  See `reduce_all_add` for
   the cost profile and `reduce` for the operator contract.


.. py:function:: reduce_add(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope sum reduction.  Result valid in **thread 0 only**.

   :param value: per-thread input.
   :param tid: calling thread's block-local index.
   :param block_dim: threads per block (template, multiple of ``2**log2_warp``).
   :param log2_warp: ``log2(warp_size)``; 5 on CUDA / Metal / RDNA, 6 on CDNA AMDGPU.
   :param dtype: scalar dtype for the inter-warp shared-memory staging slot (must match ``value``'s type).


.. py:function:: reduce_min(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope min reduction.  Result valid in **thread 0 only**.  See `reduce_add` for the argument contract.


.. py:function:: reduce_max(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope max reduction.  Result valid in **thread 0 only**.  See `reduce_add` for the argument contract.


.. py:function:: reduce_all_add(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope sum reduction with the result broadcast to every thread.  See `reduce_add` for the cheaper
   thread-0-only variant and for the argument contract.


.. py:function:: reduce_all_min(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope min reduction broadcast to every thread.  See `reduce_all_add`.


.. py:function:: reduce_all_max(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope max reduction broadcast to every thread.  See `reduce_all_add`.


.. py:function:: inclusive_scan(value, tid, block_dim: template(), log2_warp: template(), op: template(), dtype: template())

   Block-scope inclusive scan under a generic associative ``op``.  See `inclusive_add` for the argument contract;
   this generic form additionally takes an ``op: template()`` ``@qd.func`` so callers can plug in custom monoids
   without re-implementing the per-warp + shared-mem skeleton.


.. py:function:: exclusive_scan(value, tid, block_dim: template(), log2_warp: template(), op: template(), identity, dtype: template())

   Block-scope exclusive scan under a generic associative ``op`` with explicit ``identity``.  See `exclusive_add`
   for the argument contract.  Thread 0 holds ``identity``; subsequent threads hold ``op(v[0], ..., v[i-1])``.


.. py:function:: inclusive_add(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope inclusive prefix sum.  After the call, thread ``i`` holds ``v[0] + v[1] + ... + v[i]``.

   :param value: per-thread input.
   :param tid: calling thread's block-local index (``i % block_dim``, or `block.thread_idx()`).
   :param block_dim: threads per block (template, multiple of ``2**log2_warp``).
   :param log2_warp: ``log2(warp_size)``; 5 on CUDA / Metal / RDNA, 6 on CDNA AMDGPU.
   :param dtype: scalar dtype for the inter-warp shared-memory staging slot; must match ``value``'s type.


.. py:function:: inclusive_min(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope inclusive prefix min.  See `inclusive_add` for the argument contract.


.. py:function:: inclusive_max(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope inclusive prefix max.  See `inclusive_add` for the argument contract.


.. py:function:: exclusive_add(value, tid, block_dim: template(), log2_warp: template(), dtype: template())

   Block-scope exclusive prefix sum.  After the call, thread ``i > 0`` holds ``v[0] + v[1] + ... + v[i-1]`` and
   thread 0 holds the additive identity (zero, in ``value``'s dtype, derived as ``value - value``).  See `inclusive_add`
   for the argument contract.


.. py:function:: exclusive_min(value, tid, block_dim: template(), log2_warp: template(), identity, dtype: template())

   Block-scope exclusive prefix min.  Thread 0 holds ``identity``: the caller must supply a value that is ``>=``
   every legal element of the input (typically ``+∞`` for floats, the dtype's maximum for integers).  See
   `subgroup.exclusive_min` for why this op alone takes an explicit identity.


.. py:function:: exclusive_max(value, tid, block_dim: template(), log2_warp: template(), identity, dtype: template())

   Block-scope exclusive prefix max.  Thread 0 holds ``identity``: the caller must supply a value that is ``<=``
   every legal element of the input (typically ``-∞`` for floats, the dtype's minimum for integers).  See
   `exclusive_min`.


.. py:function:: radix_rank_match_atomic_or(key, tid, block_dim: template(), log2_warp: template(), radix_bits: template(), bit_start: template(), num_bits: template(), bins, excl_prefix)

   Block-level radix rank via the atomic-OR match-and-count strategy.

   Returns the calling thread's stable rank within the block under digit ``(key >> bit_start) & ((1 << num_bits) - 1)``.

   :param key: ``u32`` key, one per thread.
   :param tid: calling thread's block-local index.
   :param block_dim: threads per block (template).  Must equal ``RADIX_DIGITS = 1 << radix_bits``: each digit gets exactly
                     one thread for the per-thread bin/excl_prefix output.
   :param log2_warp: ``log2(warp_size)`` (template).  Must currently be 5 (warp size 32) — the atomic-OR match path is
                     built around 32-lane ``i32`` ballot masks, so wave64 callers should pass 5 and arrange to launch with a
                     32-thread subgroup, or wait until a wave64 path lands.
   :param radix_bits: number of bits in the digit (template).  Typical onesweep value is 8, giving 256 digits.
   :param bit_start: starting bit of the digit (template).  Used as ``key >> bit_start``.
   :param num_bits: actual digit width in bits (template), with ``num_bits <= radix_bits``.  Bits ``[bit_start, bit_start +
                    num_bits)`` of ``key`` are extracted.
   :param bins: ``block.SharedArray((1 << radix_bits,), qd.i32)`` outparam.  After the call, ``bins[d]`` holds the count
                of keys whose digit equals ``d``.  Caller is responsible for allocating this array exactly once per kernel.
   :param excl_prefix: ``block.SharedArray((1 << radix_bits,), qd.i32)`` outparam.  After the call, ``excl_prefix[d]`` holds
                       the exclusive prefix sum of ``bins`` up to digit ``d``.  Caller allocates as for ``bins``.

   Pre/post: caller must guarantee uniform control flow on entry; the function inserts the necessary ``block.sync()``
   and ``subgroup.sync()`` retires.  After the call, ``bins`` and ``excl_prefix`` are visible to every thread without a
   further ``block.sync()`` (we sync internally before exit).

   Cost: ``~items_per_thread`` atomic_or + atomic_add per pass on shared memory + 2 ``block.sync()`` + 1 block exclusive
   scan + ``BLOCK_WARPS`` ops per thread for the column-sum upsweep.  The shared-memory footprint is
   ``2 * BLOCK_WARPS * RADIX_DIGITS`` i32 ints (16 KiB at the default ``log2_warp=5, radix_bits=8`` configuration).


